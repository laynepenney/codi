# Simple test workflow for E2E testing
name: test-e2e-simple
description: Simple workflow for end-to-end testing
steps:
  - id: step1
    action: shell
    command: echo "Starting E2E test workflow"

  - id: step2
    action: switch-model
    model: "claude-3-5-haiku-latest"
    description: "Switch to fast model for quick review"

  - id: step3
    action: ai-prompt
    prompt: "Review this simple test workflow and confirm it's working"
    description: "Test AI prompt execution"

  - id: step4
    action: shell
    command: echo "Workflow completed successfully"
